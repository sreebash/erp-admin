const apiUrl = 'http://api.echelon.ae/'; //WITH TRAILING SLASH @ END
// const apiUrl = 'http://localhost:9000/'; //WITH TRAILING SLASH @ END
// export const apiUrl = process.env.NODE_ENV === 'development' ? dev : prod;

module.exports = {
    apiUrl: apiUrl
}